# Este archivo documenta las variables. Los valores reales (públicos) van en `.env`
# (versionado). Acá no hay secretos: el Client ID es público y la URL /exec es "Cualquiera".
# Los secretos viven en Script Properties de GAS.

PUBLIC_GOOGLE_CLIENT_ID=
PUBLIC_APPS_SCRIPT_URL=
