# Copiar a `.env` y completar los valores reales.
# El OAuth Client ID (tipo Web) se crea en Google Cloud Console con
# JS origins: https://redaccion.tramasdelsur.com.ar y http://localhost:4321

PUBLIC_GOOGLE_CLIENT_ID=
PUBLIC_APPS_SCRIPT_URL=
